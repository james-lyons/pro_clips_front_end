import React from 'react';
import { connect } from 'react-redux';
import { State, Props } from './editProfile.config';

const PostComponent: React.SFC<Props> = ({}) => {

    return (
        <>
            <h1>Hi from profile edit page</h1>
        </>
    );
};

export default PostComponent;
